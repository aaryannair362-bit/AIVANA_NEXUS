# Local source guideline files

This repository contains the 15 source PDF files supplied by the user for this project. Their SHA-256 hashes match `FULL_PATHWAY_MANIFEST.json`.

Keep these files within the user's authorized project environment and applicable license/permission scope. The application uses the encoded deterministic packages for runtime decisions; the PDFs are retained locally for source verification/provenance and manual review.
